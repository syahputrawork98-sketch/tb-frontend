'use client';

/**
 * Root Page: TB (Toko Bangunan)
 * Navigasi dihandle sepenuhnya oleh middleware.ts untuk kecepatan dan keamanan.
 * Halaman ini hanya berfungsi sebagai fallback jika middleware tidak aktif.
 */
export default function RootPage() {
  return null;
}
